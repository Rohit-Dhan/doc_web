import { CustomRouterActiveClassDirective } from './custom-router-active-class.directive';

describe('CustomRouterActiveClassDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomRouterActiveClassDirective();
    expect(directive).toBeTruthy();
  });
});
